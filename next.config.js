/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['img.freepik.com',],
      },images: { unoptimized: true } ,
      output: 'export',
      
}

module.exports = nextConfig
